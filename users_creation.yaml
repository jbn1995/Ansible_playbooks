---
-  name: User Create
   hosts: 192.168.1.99
   become: yes
   tasks: 
   - name: Users
     user:
       name: "{{ item }}"
       comment: new user adding
       shell: /bin/bash
       groups: avengers
     loop:
       - jon
       - henry
